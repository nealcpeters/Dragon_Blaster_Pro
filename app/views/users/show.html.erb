<% if current_user.id == params[:id].to_i %>

  <h1><%= @user.username %>'s Profile</h1>

  <%= link_to "Maps", all_maps_path %>

  <%= link_to "My Maps", user_maps_path(@user) %>

<% else %>
  <h1>You must log in to see this page.</h1>
  <%= link_to "Home", root_path %>

<% end %>


